name: Deploy
on:
  workflow_dispatch:
    inputs:
    node_version:
      description: 'Project Node.js version'
jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo "Build API"
          sleep 10s
